# Spring Boot Mybatis ZonedTime


## H2
```
create table users (
	seq INTEGER auto_increment,
	name VARCHAR(50),
	age INTEGER,
	birthday TIMESTAMP,
	since TIMESTAMP,
	created TIMESTAMP,
	updated TIMESTAMP,
	PRIMARY KEY (seq)
)
```